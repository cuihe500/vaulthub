# VaultHub Configuration File
# Environment variables can override any setting below
# Format: SECTION_KEY (e.g., SERVER_HOST, DATABASE_PORT)

[server]
host = "0.0.0.0"
port = 8080
mode = "debug"  # debug, release, test

[database]
driver = "mysql"  # mysql, postgres
host = "10.0.2.101"
port = 3306
name = "vaulthub"
user = "root"
password = "Ch870219176!"

[security]
jwt_secret = "change-me-in-production"
jwt_expiration = 24  # JWT token expiration in hours
encryption_key = "change-me-in-production-must-be-32-bytes"
casbin_model_path = "/root/project/vaulthub/configs/rbac_model.conf"

[redis]
host = "10.0.2.104"
port = 6379
password = ""  # Redis password, leave empty if no password
db = 0  # Redis database number

[logger]
level = "info"  # debug, info, warn, error, fatal
encoding = "console"  # json, console
output_paths = ["stdout"]
error_output_paths = ["stderr"]
