# VaultHub Docker 环境变量配置示例
# 复制此文件为 .env 并修改为实际值
# 使用: docker run --env-file .env vaulthub:latest

# ========================================
# Server 服务器配置
# ========================================
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
SERVER_MODE=release

# ========================================
# Database 数据库配置
# ========================================
DATABASE_DRIVER=mysql
DATABASE_HOST=mysql
DATABASE_PORT=3306
DATABASE_NAME=vaulthub
DATABASE_USER=vaulthub
DATABASE_PASSWORD=your_secure_password_here

# ========================================
# Redis 缓存配置
# ========================================
# 部署模式: standalone(单机), sentinel(哨兵), cluster(集群)
REDIS_MODE=standalone

# 单机模式配置
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# 哨兵模式配置（仅在REDIS_MODE=sentinel时生效）
# REDIS_MASTER_NAME=mymaster
# REDIS_SENTINELS=sentinel1:26379,sentinel2:26379,sentinel3:26379
# REDIS_SENTINEL_PASSWORD=

# 集群模式配置（仅在REDIS_MODE=cluster时生效）
# REDIS_ADDRS=node1:7000,node2:7001,node3:7002

# ========================================
# Security 安全配置
# ========================================
# JWT签名密钥，生产环境必须修改为强随机字符串（建议32字符以上）
SECURITY_JWT_SECRET=your-jwt-secret-key-change-in-production
# JWT令牌过期时间，单位：小时
SECURITY_JWT_EXPIRATION=24
# 数据加密密钥，必须是64字符的十六进制字符串（32字节）
SECURITY_ENCRYPTION_KEY=your-64-char-hex-encryption-key-change-in-production
# Casbin权限模型文件路径
SECURITY_CASBIN_MODEL_PATH=/app/configs/rbac_model.conf
# 超级管理员账号（首次启动时自动创建，留空则不创建）
SECURITY_ADMIN_USERNAME=admin
# 超级管理员密码（首次启动时自动创建，生产环境必须修改为强密码）
SECURITY_ADMIN_PASSWORD=Admin@StrongPassword123

# ========================================
# Logger 日志配置
# ========================================
# 日志级别: debug, info, warn, error, fatal
LOGGER_LEVEL=info
# 日志编码格式: json(JSON格式), console(控制台格式)
LOGGER_ENCODING=json
# 日志输��路径，逗号分隔多个路径（stdout表示标准输出）
LOGGER_OUTPUT_PATHS=stdout
# 错误日志输出路径，逗号分隔多个路径（stderr表示标准错误输出）
LOGGER_ERROR_OUTPUT_PATHS=stderr

# ========================================
# Audit 审计配置
# ========================================
# 审计日志缓冲区大小
AUDIT_BUFFER_SIZE=5000
# 审计日志worker数量
AUDIT_WORKER_COUNT=5
# 审计Details字段最大长度（字节）
AUDIT_MAX_DETAIL_SIZE=65000
